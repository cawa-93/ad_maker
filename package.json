{
  "name": "ad_maker-electron",
  "version": "0.1.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "start": "./node_modules/.bin/electron ./app",
    "build": "./node_modules/.bin/electron-packager ./app $npm_package_name --all --version=$npm_package_electronVersion --app-version=$npm_package_version --out=build --ignore=build --prune",
    "build:dev": "./node_modules/.bin/electron-packager ./app --version=$npm_package_electronVersion --out=build --ignore=build --overwrite"
  },
  "author": "Alex Kozack",
  "license": "GPL-3.0",
  "electronVersion": "1.4.12",
  "devDependencies": {
    "electron-installer-windows": "^0.2.0",
    "electron-packager": "^8.4.0",
    "electron-prebuilt": "^1.4.12"
  },
  "dependencies": {}
}
